# HyperMem Colab Notebook

Open `HyperMem_Qwen14B_Debug_Run.ipynb` in Colab.

Run `Runtime > Run all`.

Notebook is debug-only by default.
Full baseline is gated behind `RUN_FULL_BASELINE = False`.

VLE is not implemented in this notebook.
